# Initial build validation

This file exists to give the initial Dataarchy implementation a pull-request validation point. The CI workflow installs dependencies and executes the production Next.js build against the same application code currently on `main`.
